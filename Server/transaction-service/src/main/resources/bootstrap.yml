server:
  port: 8081
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#  instance:
#    instance-id: ${spring.application.name}-${random.int}
spring:
  application:
    name: transaction